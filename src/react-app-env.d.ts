// eslint-disable-next-line
/// <reference types="react-scripts" />
interface Goods {
  id: number;
  name: string;
  color: string;
}

interface TypeButton {
  id: number;
  title: string;
  event: () => void;
}
