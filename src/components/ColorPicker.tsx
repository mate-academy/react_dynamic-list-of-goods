import React from 'react';

// const colors = ['red', 'green', 'blue'];

export const ColorPicker: React.FC = () => {
  return (
    <button
      type="button"
      className=""
    >
      Hello!
    </button>
  );
};
