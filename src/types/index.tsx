export enum LoadError {
  LoadingError = 'Failed to load goods due to a network error!',
}

export enum GoodsType {
  Default = 'default',
  All = 'all',
  FirstFive = 'firstFive',
  OnlyRed = 'onlyRed',
}
